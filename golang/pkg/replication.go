package pkg

// #include "objectscale_client.h"
import "C"

// Storage pool is a logical construct that contains physical nodes.
type ReplicationGroup struct {
	// Indicates whether the resource is global.
	Global bool `json:"global" yaml:"global"`
	// Indicates whether the resource is remote.
	Remote bool `json:"remote" yaml:"remote"`
	// Varray mappings
	VarrayMappings []VarrayMapping `json:"varrayMappings" yaml:"zone_mappings"`
	// Unique name identifying this classification of replication group. Required. Updatable
	Name string `json:"name" yaml:"name"`
	// Identifier that is generated by ECS when the resource is created. The resource Id is guaranteed to be unique and immutable across all virtual data centers for all time.
	Id string `json:"id" yaml:"id"`
	// Timestamp that shows when this resource was created in ECS
	CreationTime int64 `json:"creation_time" yaml:"creation_time"`
	// Indicates whether the resource is inactive. When a user removes a resource, the resource is put in this state before it is removed from the ECS database.
	Inactive bool `json:"inactive" yaml:"inactive"`
	// Indicated whether the resource is an internal resource
	Internal bool `json:"internal" yaml:"internal"`
	// Description of the replication group. Updatable
	Description string `json:"description" yaml:"description"`
	// Parameter to check if the Vpool can access all Namespace. Updatable
	IsAllowAllNamespaces bool `json:"isAllowAllNamespaces" yaml:"isAllowAllNamespaces"`
	// Parameter to check if the rebalancing is enabled. Updatable
	EnableRebalancing bool `json:"enable_rebalancing" yaml:"enable_rebalancing"`
	// Parameter to check if to use replication targets
	UseReplicationTarget bool `json:"useReplicationTarget" yaml:"useReplicationTarget"`
	// set full replication flag. Non-updatable
	IsFullRep bool `json:"isFullRep" yaml:"isFullRep"`
}

type VarrayMapping struct {
	// Virtual data center id
	Name string `json:"name" yaml:"name"`
	// Storage pool id
	Value string `json:"value" yaml:"value"`
	// is replication target
	IsReplicationTarget bool `json:"is_replication_target" yaml:"is_replication_target"`
}
